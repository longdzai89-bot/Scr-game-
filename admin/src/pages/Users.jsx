import React,{useEffect,useState}from'react';import{api}from'../api.jsx';export default function Users(){const[u,setU]=useState([]);useEffect(()=>{api('/admin/users').then(setU)},[]);return <div><h2>Users</h2>{u.map(x=><p key={x.id}>{x.email} — {x.banned?'BANNED':'OK'} <button onClick={()=>api('/admin/users/'+x.id+'/ban',{method:'POST',body:'{}'}).then(()=>location.reload())}>Ban/Unban</button></p>)}</div>}
