import {Router} from 'express'; import {requireAuth,requireAdmin} from '../middleware/auth.js'; import {adminController} from '../controllers/admin.js'; const r=Router();r.use(requireAuth,requireAdmin);r.get('/users',adminController.users);r.post('/users/:id/ban',adminController.ban);r.post('/users/:id/reset-password',adminController.reset);r.get('/games',adminController.games);r.delete('/games/:id',adminController.kill);r.get('/stats',adminController.stats);r.post('/server/:action',adminController.server);r.get('/logs',adminController.logs);export default r;
