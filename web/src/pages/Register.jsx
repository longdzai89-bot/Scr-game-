import React,{useState}from'react';import{api}from'../api/client.jsx';export default function Register(){const[e,setE]=useState(''),[p,setP]=useState(''),[n,setN]=useState('');return <form onSubmit={async x=>{x.preventDefault();await api('/auth/register',{method:'POST',body:JSON.stringify({email:e,password:p,name:n})});location.href='/login'}}><h2>Đăng ký</h2><input placeholder="Tên" onChange={x=>setN(x.target.value)}/><input placeholder="Email" onChange={x=>setE(x.target.value)}/><input type="password" placeholder="Mật khẩu" onChange={x=>setP(x.target.value)}/><button>Register</button></form>}
